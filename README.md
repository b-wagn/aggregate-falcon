# Experimental Fork of [Aggregate-Falcon](https://github.com/dfaranha/aggregate-falcon)

Run `python3 compare.py -N 64` for 64 signatures. This takes a few seconds.

To plot, run `python3 compare.py --plot`. This takes a few seconds.